{
  "name": "nexusnote-mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "Anthropic MCP server configuration for NexusNote debate features.",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:server": "ts-node-dev --respawn src/index.ts",
    "start:websocket": "node dist/index.js",
    "build": "tsc -p .",
    "dev:frontend": "npm run dev --workspace frontend",
    "build:frontend": "npm run build --workspace frontend"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.716.0",
    "@aws-sdk/client-dynamodb": "^3.716.0",
    "@aws-sdk/lib-dynamodb": "^3.716.0",
    "fast-xml-parser": "^4.3.6",
    "jose": "^5.9.6",
    "pino": "^9.4.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.0"
  }
}
